spring:

  datasource:
    url: jdbc:mysql://localhost:3306/scribespot
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    open-in-view: false

  application:
    name: ScribeSpot
